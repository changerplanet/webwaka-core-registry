{
  "name": "webwaka-core-registry",
  "version": "0.1.0",
  "description": "Module registry and capability spine for WebWaka platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/**/*.ts tests/**/*.ts",
    "validate-schema": "ts-node scripts/validate-schema.ts"
  },
  "keywords": ["webwaka", "module-registry", "capabilities"],
  "author": "ChangerPlanet Team <changerplanet@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}
